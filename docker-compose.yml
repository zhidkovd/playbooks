version: '3.1'

services:

        db:
                image: postgres
                restart: always
                environment:
                        POSTGRES_PASSWORD: P@ssw0rd
                volumes:
                        - /etc/postgres:/var/lib/postgresql/data

        adminer:
                image: adminer
                restart: always
                ports:
                        - "8080:8080"
